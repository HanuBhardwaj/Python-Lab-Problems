def kadane(arr):
    maximumsum = arr[0]
    sum = arr[0]
    for i in range(1, len(arr)):
        sum = max(arr[i], sum + arr[i])
        maximumsum = max(maximumsum, sum)

    return maximumsum

arr = [-2, 1, -3, 4, -1, 2, 1, -5, 4]
print("Subarray Sum:", kadane(arr))
